{
  "name": "ts-annotations",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean:build && tsc",
    "clean:build": "rm -rf build/*",
    "clean:dist": "rm -rf dist/*",
    "start": "tsc -w",
    "update-dist": "npm run clean:dist && cp -r build/lib/* dist/",
    "test": "npm run build && karma start karma.conf.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "core-js": "^2.4.1"
  },
  "devDependencies": {
    "@types/core-js": "^0.9.34",
    "@types/jasmine": "^2.5.36",
    "@types/reflect-metadata": "0.0.5",
    "browserify": "^13.1.1",
    "jasmine-core": "^2.5.2",
    "karma": "^1.3.0",
    "karma-browserify": "^5.1.0",
    "karma-phantomjs-launcher": "^1.0.2",
    "karma-jasmine": "^1.0.2",
    "karma-spec-reporter": "0.0.26",
    "typescript": "^2.0.6",
    "watchify": "^3.7.0"
  }
}
